完全确认了。现在我对落地细节有清晰把握。用户的判断是对的，核心就是这个。我给一个干脆的确认，并说明落地时一个不能漏的细节（避免他以为"删掉定时器就完了"而踩坑），然后主动提出动手。

对，你抓到本质了——核心思路就是这一条，对了。不过落地时有一个细节不能漏，不然你会以为"删了定时器就完事"，结果遇到边缘场景花屏。

你说的对，但「长 GOP」不只是关定时器
现在代码里 GOP 三档全都很短，正常网络也才 1 秒：

    private let gopNormal: Double = 1.0      // 正常网络：1秒
    private let gopWeak: Double = 0.5        // 弱网：0.5秒
    private let gopExtreme: Double = 0.5     // 极端弱网：0.5秒
如果你只是「关掉定时器」，那 GOP 就交给 WebRTC 默认（约 3 秒），这正是同类 App 的状态，OK。但有一个坑：你们的「弱网快恢复」逻辑（set_fps 的 critical/high 分支）现在是靠缩短这个定时器间隔来实现快恢复的：

            applyFpsImmediately(targetFps, bitrate: bitrate)
            forceKeyframe()
            setKeyframeInterval(gopExtreme)   // ⭐ 改间隔即重启定时器使其生效
            malvshezhingLog("[set_fps] critical \(oldFps)→\(targetFps)fps GOP=\(gopExtreme)s 码率=\(bitrate)")
        case "high":
            // ⚡ 高优先级：保码率不降，只降FPS + 插I帧
            applyFpsImmediately(targetFps, bitrate: bitrate)
            forceKeyframe()
            setKeyframeInterval(gopWeak)   // ⭐ 改间隔即重启定时器使其生效
所以「完全删掉定时器」会让弱网恢复变慢。正解是分两种状态，而不是一刀切：

落地方案（保住快恢复，常态不发热）
状态	关键帧策略	效果
常态（99% 时间）	不开定时器，纯靠 PLI 按需 + WebRTC 默认长 GOP	不发热（你要的）
真弱网/恢复瞬间（critical/high）	临时开短 GOP 定时器，几秒后自动关回常态	保住快恢复，不花屏
观看端要 I 帧（PLI）	响应一次 forceKeyframe，加 1 秒节流	按需，不被刷爆
关键就是「弱网那档用完要自动归位到常态（关定时器）」，而不是像现在 normal 分支把它设回 1 秒继续常驻。

一句话
你的方向 100% 对：常态信任 WebRTC 的「按需 PLI + 长 GOP」。唯一要加的是「真弱网时临时短 GOP、用完自动关」这个安全阀，这样既不发热、又不丢「弱网快恢复」这个原有能力。

要我现在就按这个改 WebRTCManager.swift 吗？具体动作：

常态不启动 0.5s 定时器（startStatsAndAdapt 里那行去掉）。
弱网 critical/high 分支：临时开短 GOP，3~5 秒后自动 stopKeyframeTimer() 归位。
normal/low 分支：直接关定时器（而不是设回 1 秒常驻）。
PLI / REQUEST_KEYFRAME：保留 forceKeyframe，加 1 秒节流。
SRT 不碰。
你点头我就动手。

先同步这条，再继续待命。

已同步。继续待命，接收下一条消息。




用户批准了，让我按方案改，改好后推送 git。我先建立任务清单，然后仔细读取需要修改的代码区域，做精确修改。

让我先读取关键代码区域：startStatsAndAdapt 里启动定时器那行、set_fps 分支、onRequestKeyframeCommand、关键帧定时器相关，以及确认 git 状态。

常态不启动 0.5s 定时器（startStatsAndAdapt 去掉 startKeyframeTimer）
弱网 critical/high 分支：临时开短 GOP，几秒后自动关回常态
normal/low 分支：关定时器而非设回常驻
PLI/REQUEST_KEYFRAME 加 1 秒节流
检查 lint 并验证 SRT 未受影响
git 提交并推送
First, let me check the git status and read the relevant code regions precisely.

The git repo is D:/sb/ios, branch srs-yql-fz. Now let me make the edits.

Edit 1: Remove常态 startKeyframeTimer() from startStats().

WebRTCManager.swift
+7
-3
  
  // 🔥🔥 超低延迟优化：启用关键帧定时器，每秒发送一个关键帧
  // 配合GStreamer方案：缩短GOP，丢包后最多等1秒就能恢复，减少花屏
  startKeyframeTimer()
  // ⭐ 2026-06-25 发热优化：常态【不】启动定时强制关键帧。
  //   旧实现 startKeyframeTimer() 常驻每 0.5~1s 强制一个 IDR（forceKeyframeViaBitrate 码率抖动），
  //   每个 I 帧编码算力是 P 帧数倍 → 编码器持续高负载，是 P2P/SRS 发热的主因。
Edit 2 & 3: Now the set_fps branches. I need to add auto-revert for critical/high, and make normal/low stop the timer. Let me re-read the current state of that block to edit precisely.

WebRTCManager.swift
+15
-8
  // 🔥 v10.1 防花屏：根据 urgency 决定执行方式 + 降码率 + 插I帧
  // ⭐ 2026-06-25 发热优化：弱网（critical/high）才【临时】开短 GOP 快恢复，
  //   用完由 scheduleKeyframeTimerAutoStop 在数秒后自动 stopKeyframeTimer 归位到常态；
  //   normal/low（网络已恢复/升帧）直接关定时器，回到「默认长 GOP + 按需 PLI」，不再常驻短 GOP。
  switch urgency {
      forceKeyframe()
Now I need to add the scheduleKeyframeTimerAutoStop() method. Let me add it near the keyframe timer methods. Let me view the exact region around stopKeyframeTimer and setKeyframeInterval.

There's an important subtlety: setKeyframeInterval only restarts the timer if keyframeTimer != nil. But now常态没有定时器运行，所以弱网分支调用 setKeyframeInterval(gopExtreme) 时 keyframeTimer == nil，不会启动定时器。I need to fix setKeyframeInterval to start the timer when needed, OR have the critical/high branch explicitly start it.

The cleanest approach: make critical/high branch call setKeyframeInterval then ensure timer is running. Let me update setKeyframeInterval to start the timer if it's not running (since it's now only called when we want short GOP active), and add the auto-stop method. Let me also check the keyframe interval member and add an auto-stop work item property.

WebRTCManager.swift
+20
-5
        keyframeTimer = nil
        keyframeAutoStopWork?.cancel()
        keyframeAutoStopWork = nil
    }
    /// ⭐ 点2 修复：设置关键帧间隔；若定时器正在运行则立即重启，使新间隔生效。
    /// 旧实现只改 keyframeIntervalSec 不重启 → 间隔变化要等下次开流才生效。
Now I need to declare the new properties keyframeAutoStopWork and keyframeTimerAutoStopSec. Let me add them near the existing keyframe-related properties (gopNormal etc. around line 2945, and keyframeTimer declaration).

